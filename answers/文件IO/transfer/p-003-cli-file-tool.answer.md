# 答案：p-003-cli-file-tool

## 参考实现思路
1. 用 `argparse` 解析参数：`src`、`dst`、`--exclude-ext`。
2. 构造 `ignore` 回调，根据后缀过滤。
3. 复制后用 `Path(dst).rglob('*')` 统计文件数与目录数。

## 验收提示
- 重复执行时应说明目标目录策略（清理或覆盖）。
- 输出示例至少包含：复制文件总数、目录总数。
