# 题目：p-001-path-basics

## 训练目标
理解 `Path` 与 `os.path` 的基础路径属性与拼接行为。

## 题目要求
1. 给定一个路径字符串，输出其 `name/stem/suffix/parent`。
2. 分别用 `os.path.join` 和 `Path` `/` 运算符拼接子路径。
3. 对比两种拼接结果并说明差异。

## 验收标准
- 能输出四个基础属性。
- 两种拼接方式都可运行。
- 说明中包含至少 1 条可读性或可维护性差异。
