# 题目：p-002-copytree-filter

## 训练目标
掌握 `copytree` 与 `ignore` 回调的过滤复制逻辑。

## 题目要求
1. 编写 `ignore` 回调，忽略 `.py` 与 `__pycache__`。
2. 执行目录复制并打印复制结果文件列表。
3. 说明目标目录已存在时的处理策略。

## 验收标准
- 忽略规则生效，结果目录不包含 `.py` 与 `__pycache__`。
- 能输出复制后的递归文件列表。
- 明确给出“预删除”或 `dirs_exist_ok=True` 的策略说明。
