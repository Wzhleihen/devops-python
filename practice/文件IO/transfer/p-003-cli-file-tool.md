# 题目：p-003-cli-file-tool

## 训练目标
把路径与文件操作迁移到命令行工具场景。

## 题目要求
1. 使用 `argparse` 接收 `src`、`dst`、`--exclude-ext`。
2. 实现目录复制并支持按后缀排除。
3. 输出复制统计（文件数、目录数）。

## 验收标准
- 命令行参数解析成功。
- 排除规则可控制复制结果。
- 输出包含统计信息且可重复执行。
