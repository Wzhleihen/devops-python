# AGENTS.md

本文件定义本仓库中 AI 代理（Agents）的统一工作规范。目标：提高 Python 学习效率、复盘质量与持续训练效果。

## 1) 仓库定位

这是一个**学习驱动型 Python 练习仓库**，不是发布型应用仓库。  
核心链路：**代码变更 → day 学习总结 → 强化训练（练习）→ 能力评估**。

## 2) 目录约定（当前结构）

- `归档/2025/`
  - 历史学习记录与原始代码证据（日期目录）。
  - 作为 day 生成的主要事实来源之一。
- `daily/`
  - 学习日记输出目录（`day01.md`, `day02.md`...）。
  - `daily/templates/` 存放模板。
- `07_面向对象/`、`文件IO/`、`序列化与反序列化/`、`正则表达式/`、`作业/`
  - 章节学习与练习主目录（中文章节名保留）。
- `openspec/`
  - 需求、设计、任务拆解与变更流程管理。

## 3) 命名与语言规则

- **章节目录中文名保留，不做无必要改动**。
- 新增文件名优先英文语义化命名：
  - Python: `snake_case.py`
  - Markdown: `kebab-case.md`
- 避免新增低语义文件名（如 `demo01.py`, `t1.py`），除非明确用于课堂临时演示。

## 4) Day 生成规范（强约束）

1. `dayxx.md` 必须按时间线连续（`day01..dayNN`）。
2. day 内容优先基于：
   - `归档/`中的原始代码与笔记；
   - Git 历史（提交信息、日期聚合）。
3. 每个 day 必须是**完整 AI 分析**，不可只有占位描述。
4. 每个 day 必须包含：
   - 章节内容与学习位置
   - 核心知识原理（机制层）
   - 至少 1 个真实错误案例（现象/根因/修正/防再犯）
   - 关键代码片段与行为解释
   - 下一步可执行学习计划
   - 回链路径（代码、练习、答案、归档来源）

## 5) 练习策略（关键原则）

练习不是题目堆叠，定位为：**强化训练**。

- 每个 day 至少产出 1 题练习；
- 练习按三层难度组织：
  - 基础（巩固）
  - 进阶（组合应用）
  - 迁移（跨章节迁移）
- 每题必须有：
  - 训练目标
  - 明确要求
  - 可验证验收标准
  - 参考答案路径（或答案说明）

## 6) Agents 工作边界

- 优先小步修改、保持可回滚。
- 不随意搬动章节目录内部文件，除非用户明确要求。
- 不做与当前学习目标无关的大规模重构。
- 修改模板后，需保证既有 day 文档可平滑迁移。

## 7) OpenSpec 工作流

涉及需求/设计/任务时，优先使用 OpenSpec：

1. `propose`：明确变更目标（why/what）
2. `design`：明确实现方案（how）
3. `tasks`：可执行任务清单
4. `apply`：按任务逐项实现
5. `archive`：完成后归档变更

## 8) 质量检查清单

每次产出 day 或练习后应检查：

- 编号是否连续；
- 路径是否真实可定位；
- 模板必填项是否完整；
- 错误案例是否真实且可复现；
- 练习是否具备验收标准；
- 内容是否确实来自本仓库学习证据（归档/代码/历史）。

## 9) 协作风格

- 默认中文沟通；
- 输出先给结论，再给证据路径；
- 对不确定内容明确标注“待确认”，不伪造事实。
